# shared_libraries/CMakeLists.txt
# =============================================
# Shared third-party libraries directory
# =============================================

# Third-party libraries (FreeImage, NanoSVG, SoLoud) are included directly
# by dependent tools or discovered via root CMake configuration.
